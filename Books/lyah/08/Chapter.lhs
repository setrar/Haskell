> module Chapter09 where
> import Control.Monad

> s = sequence $ map print [1,2,3,4]

type s at the prompt to execute the expression

> lM = mapM_ print [1,2]

type lM at the prompt to execute the expression

> mM = forM_ [1,2] print

type mM at the prompt to execute the expression
